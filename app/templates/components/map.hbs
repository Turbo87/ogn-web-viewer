<div class="map-wrapper">
  <div id="map" style="width: 100%; height: {{if (and hasDeviceFilter @task) "70%" "100%"}}">
    <TaskLayer @task={{@task}}/>
  </div>
  {{#if (and hasDeviceFilter @task)}}
    <div class="scores-wrapper">
      <ScoreTable @task={{@task}}/>
    </div>
  {{/if}}
</div>